# AuthApi Tests

This project contains unit tests for the AuthApi project using:
- NUnit (test framework)
- NSubstitute (mocking library)
- FluentAssertions 4.x (assertions library)

## Running the Tests

### Using the command line
